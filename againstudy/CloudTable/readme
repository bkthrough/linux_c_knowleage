云表，就是分布式的一个关系型数据库
构成：
1.Master：相当于一个管家，管理节点和表
2.region：
（1）wal日志，为了防止中途的事件，把所有的操作都写一遍到wal日志中，在开启
云表的时候去查看wal日志的操作是否进行过，如果没有，那么就去进行
（2）tablet，tablet包括memstore（用于存储新的数据）， yfile（对较旧的数据
写入磁盘）
Memstore内置一个巨大的数组来存储最新的数据，但当其存储的数据达到预先设定
的阀值时，这个数组中的数据会被取出，并写入一个新创建的YFile中。
