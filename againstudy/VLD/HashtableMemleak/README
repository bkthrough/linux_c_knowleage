有一个很需要注意的问题，就是如果全局变量放置在头文件，那么会导致多个文件
调用该头文件时发生重定义现象。
我的解决方法：还是在头文件定义，不过每个引用该头文件的文件没有耦合。
就是说把cpp和c的完全分开，在cpp的文件中加上c的代码，用的时候只用c/cpp即可
这样做比较简单，但是设计的不是很好，扩展性不强！！

c和cpp的加载方式不一样


如果是c文件，那么就使其包含vld_c.h头文件，并且在编译时 -lvldc
如果是cpp文件，包含vld.h头文件，在编译是 -lvldcpp

